node_modules/
dist/
.DS_Store
# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual environment
.venv/
venv/
env/

# Environment variables / secrets
.env
.env.*
!.env.example

# HuggingFace model cache (if ever redirected into the project)
.cache/

# Generated sample outputs — test images and scripts are kept, outputs are not
sample/response.json
sample/overlay.png

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# Backend caches — keep only the pre-warmed s_synthesizer artifacts for the demo
server/cache/imagery/
server/cache/footprints/
server/cache/geocode/
server/cache/gemini_pitch_multi/
server/cache/llm_pitch_multi/
server/cache/llm_regional/
server/cache/osm/
server/cache/solar_building_data/
server/cache/streetview/
server/cache/streetview_images/
server/cache/streetview_pitch/
server/cache/synthesizer/
server/outputs/

# Python build artifacts
*.egg-info/
.pytest_cache/

# Local-only working files (not for the public repo)
CLAUDE.md
project-details/
